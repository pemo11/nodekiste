extends layout

block content
    table.table
      tr
       th Name
       th E-Mail
       th Stadt
       th Land
      tbody
       each user in users
        tr
         td #{user.username}
         td #{user.email}
         td #{user.city}
         td #{user.country}
